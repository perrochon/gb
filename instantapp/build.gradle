apply plugin: 'com.android.instantapp' // TODO This all needs to be fixed for the structur changes. Or not

dependencies {
    implementation project(':feature')
    implementation project(':base')

}
